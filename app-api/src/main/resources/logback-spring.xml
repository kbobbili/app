<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <springProperty name="rootLevel" source="logging.level.root" defaultValue="WARN"/>
  <springProperty name="springLevel" source="logging.level.spring" defaultValue="INFO"/>
  <springProperty name="appLevel" source="logging.level.app" defaultValue="DEBUG"/>

  <!-- referring spring default values & console appender locally -->
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
  <!--<include resource="org/springframework/boot/logging/logback/console-appender.xml"/>-->
  <!--overriding spring's console log pattern-->
  <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{HH:mm:ss.SSS}){faint} %clr(%5p) %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${CONSOLE_LOG_PATTERN}</pattern>
      <charset>utf8</charset>
    </encoder>
  </appender>

  <springProfile name="default,local">
    <logger name="com.kubra.iq" level="${appLevel}" additivity="false">
      <appender-ref ref="CONSOLE" />
    </logger>
    <logger name="com.kubra.security" level="${appLevel}" additivity="false">
      <appender-ref ref="CONSOLE" />
    </logger>
    <logger name="org.springframework" level="${springLevel}" additivity="false">
      <appender-ref ref="CONSOLE" />
    </logger>
    <root level="${rootLevel}">
      <appender-ref ref="CONSOLE" />
    </root>
  </springProfile>

  <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
    <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
      <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
        <prettyPrint>false</prettyPrint>
      </jsonFormatter>
      <appendLineSeparator>true</appendLineSeparator>
      <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSSX</timestampFormat>
      <timestampFormatTimezoneId>Etc/UTC</timestampFormatTimezoneId>
    </layout>
  </appender>

  <springProfile name="dev,qa,staging,prod">
    <logger name="com.kubra.iq" level="${appLevel}" additivity="false">
      <appender-ref ref="JSON" />
    </logger>
    <logger name="com.kubra.security" level="${appLevel}" additivity="false">
      <appender-ref ref="JSON" />
    </logger>
    <logger name="org.springframework" level="${springLevel}" additivity="false">
      <appender-ref ref="JSON" />
    </logger>
    <logger name="org.springframework.cloud.config" level="DEBUG" additivity="false">
      <appender-ref ref="JSON" />
    </logger>
    <root level="${rootLevel}">
      <appender-ref ref="JSON" />
    </root>
  </springProfile>

</configuration>